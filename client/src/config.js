// Deployment mode controls which views and features are available.
// Change this value to switch between deployment scenarios.
//
//   'restaurant' — full-service restaurant (floor + bar tabs)
//   'bar'        — quick bar / no table service (bar tabs only)
//   'hotel'      — hotel restaurant (adds room charge payment option)
//
export const APP_MODE = 'restaurant';
